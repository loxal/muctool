---
applications:
  - name: muctool
    memory: 128M
    instances: 1
    #  no-route: true
    #  random-route: true
    stack: cflinuxfs3
    #  buildpack: https://github.com/cloudfoundry/java-buildpack
    #  buildpack: java_buildpack
    #  path: ./service/build/libs/service-1.0.0.jar
    #  command: sleep 60; ls -lash /
    docker:
      image: loxal/muctool
    #    username: docker-user-name
    env:
      SECURITY_USER_PASSWORD: Munich
      SPRING_PROFILES_ACTIVE: cloud,prod
